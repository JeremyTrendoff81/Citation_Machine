/**
 * The Actions Enum. Provides a set of actions that controller must account for.
 * @author Jeremy Trendoff - 101160306
 */
public enum Actions {
    SET_FIRST_NAME,
    SET_LAST_NAME,
    IEEE_SELECT,
    ACM_SELECT,
    FORMAT
}
